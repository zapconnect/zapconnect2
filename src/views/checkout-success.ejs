<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <title>Pagamento Aprovado • ZapConnect</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
        crossorigin="anonymous" referrerpolicy="no-referrer">

    <style>
        :root {
            --bg: #0D1222;
            --card: #161B30;
            --border: #373E59;
            --text: #FFFFFF;
            --muted: #AAB0D9;
            --primary: #6C64EF;
            --primary-hover: #8B5CF6;
            --success: #2EE6A6;
        }

        * {
            box-sizing: border-box;
            font-family: Inter, system-ui, sans-serif;
        }

        body {
            margin: 0;
            min-height: 100vh;
            background: radial-gradient(circle at top, #151a3a, var(--bg));
            color: var(--text);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 16px;
        }

        .success-card {
            background: linear-gradient(180deg, #181f3d, #12172e);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 36px 32px;
            width: 100%;
            max-width: 420px;
            text-align: center;
            box-shadow: 0 20px 60px rgba(108, 100, 239, .35);
            animation: fadeIn .4s ease;
        }

        .icon {
            width: 72px;
            height: 72px;
            border-radius: 50%;
            background: rgba(46, 230, 166, .15);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
        }

        .icon i {
            font-size: 34px;
            color: var(--success);
        }

        h1 {
            margin: 0;
            font-size: 22px;
            font-weight: 700;
        }

        p {
            margin-top: 10px;
            font-size: 14px;
            color: var(--muted);
            line-height: 1.6;
        }

        .btn {
            margin-top: 28px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            width: 100%;
            padding: 14px;
            border-radius: 14px;
            border: none;
            background: linear-gradient(135deg, var(--primary), var(--primary-hover));
            color: #fff;
            font-size: 15px;
            font-weight: 700;
            text-decoration: none;
            cursor: pointer;
            box-shadow: 0 0 30px rgba(108, 100, 239, .45);
            transition: .2s;
        }

        .btn:hover {
            filter: brightness(1.15);
        }

        .btn:active {
            transform: scale(0.97);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(14px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* =========================
   ANIMAÇÃO ÍCONE SUCESSO
========================= */
        .icon {
            animation: popIn 0.6s ease-out forwards;
        }

        .icon i {
            animation: pulseSuccess 1.8s ease-in-out infinite;
        }

        @keyframes popIn {
            0% {
                transform: scale(0.5);
                opacity: 0;
            }

            70% {
                transform: scale(1.1);
                opacity: 1;
            }

            100% {
                transform: scale(1);
            }
        }

        @keyframes pulseSuccess {
            0% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.12);
            }

            100% {
                transform: scale(1);
            }
        }

        /* =========================
   BARRA DE PROGRESSO
========================= */
        .progress-wrap {
            margin-top: 22px;
        }

        .progress-text {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            font-size: 13px;
            color: var(--muted);
            margin-bottom: 10px;
        }

        .progress-bar {
            width: 100%;
            height: 10px;
            background: rgba(255, 255, 255, .08);
            border-radius: 999px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            width: 0%;
            background: linear-gradient(135deg,
                    var(--primary),
                    var(--primary-hover));
            border-radius: 999px;
            transition: width 0.4s ease;
        }

        .progress-percent {
            display: block;
            margin-top: 8px;
            font-size: 12px;
            text-align: center;
            color: var(--muted);
        }
    </style>
</head>

<body>

    <div class="success-card">

        <div class="icon">
            <i class="fa-solid fa-circle-check"></i>
        </div>

        <h1>Pagamento aprovado</h1>

        <p>
            Seu pagamento foi confirmado com sucesso.<br>
            O plano será ativado automaticamente em instantes.
        </p>
        <div class="progress-wrap">
            <div class="progress-text">
                <i class="fa-solid fa-gear fa-spin"></i>
                Ativando seu plano…
            </div>

            <div class="progress-bar">
                <div class="progress-fill" id="progressFill"></div>
            </div>

            <span class="progress-percent" id="progressPercent">0%</span>
        </div>

        <a href="/painel" class="btn">
            <i class="fa-solid fa-arrow-right"></i>
            Ir para o painel
        </a>

    </div>
    <script>
        let progress = 0;
        const fill = document.getElementById("progressFill");
        const percent = document.getElementById("progressPercent");

        const interval = setInterval(() => {
            if (progress >= 100) {
                clearInterval(interval);
                percent.innerText = "Plano ativado!";
                return;
            }

            progress += Math.floor(Math.random() * 8) + 4; // progresso orgânico
            if (progress > 100) progress = 100;

            fill.style.width = progress + "%";
            percent.innerText = progress + "%";
        }, 600);
    </script>


</body>

</html>